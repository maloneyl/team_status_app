.section-heading
  Reports

%h3 Today

- if @trackable_items_today.present?
  = pie_chart @trackable_items_today.group(:body).sum(:time_tracked)
  .users-reports-today-list
    - @trackable_items_today.sort_by(&:updated_at).reverse.each do |status|
      .users-reports-today-list-item
        = status.body
        %br/
        = link_to status.group.name, group_path(status.group)
        \|
        %i= distance_of_time_in_words(status.time_tracked)
        %i= "and counting" if status.tracking?
- else
  Nothing here...

%h3 Yesterday

- if @trackable_items_yesterday.present?
  = pie_chart @trackable_items_yesterday.group(:body).sum(:time_tracked)
  .users-reports-yesterday-list
    - @trackable_items_yesterday.sort_by(&:updated_at).reverse.each do |status|
      .users-reports-yesterday-list-item
        = status.body
        %br/
        = link_to status.group.name, group_path(status.group)
        \|
        %i= distance_of_time_in_words(status.time_tracked)
        %i= "and counting" if status.tracking?
- else
  Nothing here...
