- if user_signed_in?
  - if current_user == @user
    = render 'groups'
    = render 'mentions'
    = render 'agendas'
    = render 'report'
  - else
    This is not the dashboard you're looking for...
- else
  Please sign up or sign in.
