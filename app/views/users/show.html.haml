- if user_signed_in?
  - if current_user == @user
    = render 'groups'
    = render 'mentions'
    = render 'agenda'
    = render 'report'
    %p Join a group
  - else
    This is not the dashboard you're looking for...
- else
  Please sign up or sign in.
