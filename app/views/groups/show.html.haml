/ :javascript
/   setTimeout(function(){
/     location = ''
/   },4000)
// 4000 = 4 seconds

- if user_signed_in?
  - if @is_member == true
    %h2= @group.name.titleize
    = render 'members'
    = render 'agendas'
    = render 'statuses'
    - if @is_owner == true
      %p= link_to 'Edit Group/Members', edit_group_path(@group)
  - else
    This is not the group you're looking for...
- else
  Please sign up or sign in.

